## What this changes

<!-- What behaviour is different after this, from the user's side. -->

## Why

<!-- The problem it solves. If it fixes an issue, link it. -->

## How it was verified

<!-- What you ran, and what it printed. "Tests pass" on its own is not enough:
     say which behaviour is now covered that was not before. -->

- [ ] `cd backend && make vet`
- [ ] `cd backend && make test`
- [ ] `cd frontend && npx tsc --noEmit && npm run build`

## Notes

<!-- Anything a reviewer would otherwise have to work out: a convention you had
     to break, something left deliberately undone, a format quirk you found. -->
